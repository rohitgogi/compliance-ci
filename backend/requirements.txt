# Compliance CI Backend
# Install: pip install -r requirements.txt
# Required for Render web service deployment

# Web framework
fastapi>=0.116.0
uvicorn[standard]>=0.32.0

# File upload (required for UploadFile / multipart form parsing)
python-multipart>=0.0.9

# API / HTTP
httpx>=0.28.0
groq>=0.11.0

# Data validation
pydantic>=2.11.0

# YAML parsing
PyYAML>=6.0.2

# Database / vector (optional: pgvector only when COMPLIANCE_PGVECTOR_DSN set)
pgvector>=0.3.4
psycopg[binary]>=3.2.0

# Embeddings (optional: only when pgvector enabled)
sentence-transformers>=3.0.0
torch>=2.4.0
